name: admin_panel
description: Admin Dashboard for Ordarat System
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  # ئەم دوو پاکێجە چارەسەری ئێرۆرەکەی گیتهاپ دەکەن
  firebase_storage: ^11.5.6
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
